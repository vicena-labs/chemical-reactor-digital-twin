# Contributing

Install with `python -m pip install -e .[dev]`, run `python -m unittest discover -s tests -v`, and update tests, schemas, documentation, and CHANGELOG.md together. New chemistry must include parameter provenance, units, validity range, conservation checks, and safety limitations. Do not contribute proprietary data or unsafe operating instructions.
